{
  "name": "ai-worker-platform-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Vue2 frontend for AI Worker Platform PR Worker MVP.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "smoke": "node scripts/route-smoke.js",
    "test": "npm run build && npm run smoke"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "vue": "^2.7.16",
    "vue-router": "^3.6.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue2": "^2.3.4",
    "vite": "^7.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
