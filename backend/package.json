{
  "name": "ai-worker-platform-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Express backend for AI Worker Platform PR Worker MVP.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "health": "node -e \"fetch('http://localhost:8000/health').then(r=>r.json()).then(console.log)\"",
    "smoke": "node scripts/smoke-test.js",
    "test:preflight": "node scripts/preflight-test.js",
    "test:integration": "node scripts/integration-test.js",
    "test:queue-registry": "node scripts/queue-registry-test.js",
    "test:ran-adapter": "node scripts/ran-adapter-test.js",
    "test": "npm run smoke && npm run test:preflight && npm run test:integration && node scripts/error-visibility-test.js && npm run test:ran-adapter && npm run test:queue-registry"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "exceljs": "^4.4.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "ws": "^8.18.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "engines": {
    "node": ">=20"
  }
}
